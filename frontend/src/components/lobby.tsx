import Overlay from "./overlay";

const Lobby = () => {
  return (
    <Overlay className="inset-y-12 text-3xl">
      <div>Waiting for Other Players</div>
    </Overlay>
  );
};

export default Lobby;
